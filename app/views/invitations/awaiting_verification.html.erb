<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 flex items-center justify-center p-4">
  <div class="max-w-md w-full bg-white rounded-2xl shadow-xl p-8">
    <div class="text-center mb-8">
      <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
        <%= icon :mail, class: "w-8 h-8 text-green-600" %>
      </div>
      <h1 class="text-2xl font-bold text-gray-900 mb-2">Check je Email!</h1>
    </div>

    <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-6">
      <p class="text-blue-900 font-medium mb-2">
        We hebben een verificatie email gestuurd naar:
      </p>
      <p class="text-blue-600 font-mono text-lg break-all">
        <%= @invitation.email %>
      </p>
    </div>

    <div class="space-y-4 text-sm text-gray-600 mb-6">
      <p class="flex items-start gap-2">
        <span class="text-blue-600 font-bold">1.</span>
        Open je email inbox
      </p>
      <p class="flex items-start gap-2">
        <span class="text-blue-600 font-bold">2.</span>
        Klik op de verificatie link
      </p>
      <p class="flex items-start gap-2">
        <span class="text-blue-600 font-bold">3.</span>
        Je wordt automatisch doorgestuurd
      </p>
    </div>

    <div class="p-4 bg-yellow-50 border border-yellow-200 rounded-lg text-sm text-yellow-800">
      <strong>Tip:</strong> Check ook je spam folder als je niets ziet.
    </div>

    <div class="mt-6 text-center text-sm text-gray-500">
      <p>Geen email ontvangen?</p>
      <%= link_to "Probeer opnieuw", open_invitation_path(@invitation.token), class: "text-blue-600 hover:text-blue-700 font-medium" %>
    </div>
  </div>
</div>
